[project]
name = "agent-trust-harness"
version = "0.1.0"
description = "Integration test harness for agent trust lifecycle"
requires-python = ">=3.10"

[project.optional-dependencies]
test = ["pytest>=7.0"]
